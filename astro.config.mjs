// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // GitHub Pages deployment
  site: 'https://aidencullo.github.io',
  base: '/personal-website',
});
