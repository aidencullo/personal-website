// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // GitHub Pages deployment (uncomment these lines if using GitHub Pages)
  // site: 'https://aidencullo.github.io',
  // base: '/personal-website',

  // Vercel/Netlify deployment (default - no base needed)
  // Update this URL after deploying to your chosen platform
  site: 'https://personal-website.vercel.app',
});
