// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // GitHub Pages deployment (uncomment and set your repo name if using GitHub Pages)
  // site: 'https://yourusername.github.io',
  // base: '/your-repo-name',

  // Vercel/Netlify deployment (default - no base needed)
  site: 'https://yourname.com',
});
